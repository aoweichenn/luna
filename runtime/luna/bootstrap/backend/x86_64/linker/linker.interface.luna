export module luna.bootstrap.backend.x86_64.linker;

import luna.runtime;
import luna.std.bytes;
import luna.bootstrap.backend.x86_64.object;

export struct BootstrapLinkerResult {
    executable: StdByteBuffer;
    error: RuntimeError;
    input_index: usize;
}

export fn bootstrap_x86_64_link(
    objects: *const BootstrapObject,
    object_count: usize
) -> BootstrapLinkerResult;

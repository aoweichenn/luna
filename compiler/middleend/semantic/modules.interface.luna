export module luna.bootstrap.middleend.semantic.modules;

import luna.runtime;
import luna.std.bytes;
import luna.std.text;
import luna.bootstrap.frontend.lexer;
import luna.bootstrap.frontend.parser;
import luna.bootstrap.middleend.type;
import luna.bootstrap.middleend.ir;
import luna.bootstrap.middleend.semantic.context;

export fn bootstrap_semantic_collect_modules( context: *BootstrapSemanticContext ) -> bool;

export fn bootstrap_semantic_collect_unit_imports( context: *BootstrapSemanticContext, module_id: usize, unit_index: usize, interface_import: bool ) -> bool;

export fn bootstrap_semantic_collect_imports( context: *BootstrapSemanticContext ) -> bool;

export fn bootstrap_semantic_visit_imports( context: *BootstrapSemanticContext, module_id: usize, depth: usize ) -> bool;

export fn bootstrap_semantic_validate_import_graph( context: *BootstrapSemanticContext ) -> bool;

export fn bootstrap_semantic_symbols_have_same_name( context: *BootstrapSemanticContext, left: BootstrapSemanticSymbol, right: BootstrapSemanticSymbol ) -> bool;

export fn bootstrap_semantic_validate_imported_names( context: *BootstrapSemanticContext ) -> bool;

export fn bootstrap_semantic_mark_reachable( context: *BootstrapSemanticContext, module_id: usize, depth: usize ) -> bool;

export fn bootstrap_semantic_validate_reachability( context: *BootstrapSemanticContext, root_module: usize ) -> bool;

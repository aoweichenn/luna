module test.nested_readonly_pointer_cast;

fn main() -> i32 {
    var value: i32 = 1;
    var inner: *const i32 = (&value) as *const i32;
    let nested: **const i32 = &inner;
    let invalid: **i32 = nested as **i32;
    return **invalid;
}

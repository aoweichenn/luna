module test.zero_initializer_scalars;

fn main() -> i32 {
    let flag: bool = {};
    let integer: i64 = {};
    let single: f32 = {};
    let double: f64 = {};
    let pointer: *i32 = {};

    if (flag || integer != 0 || single != 0.0 || double != 0.0 ||
        pointer != null) {
        return 1;
    }
    return 42;
}

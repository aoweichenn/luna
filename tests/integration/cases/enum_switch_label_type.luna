module tests.enum_switch_label_type;

enum Kind: u8 {
    value,
}

fn main() -> i32 {
    let kind: Kind = Kind.value;
    switch (kind) {
        case 0 {
            return 1;
        }
        default {
            return 0;
        }
    }
}

export module tests.metadata.core;

export struct Sample {
    value: i32;
}

export enum Bias: i32 {
    one = 1,
}

export fn add_bias(input: *const Sample, bias: Bias,
                   first: i32, second: i32, third: i32,
                   fourth: i32, fifth: i32) -> i32;

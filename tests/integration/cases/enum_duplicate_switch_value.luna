module tests.enum_duplicate_switch_value;

enum Kind: u8 {
    first = 7,
    second = 7,
}

fn main() -> i32 {
    let kind: Kind = Kind.first;
    switch (kind) {
        case Kind.first {
            return 1;
        }
        case Kind.second {
            return 2;
        }
    }
}

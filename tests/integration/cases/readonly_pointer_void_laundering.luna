module test.readonly_pointer_void_laundering;

fn main() -> i32 {
    var value: i32 = 1;
    var inner: *const i32 = (&value) as *const i32;
    let nested: **const i32 = &inner;
    let invalid: *void = nested as *void;
    return 0;
}

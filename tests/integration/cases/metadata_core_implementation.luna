module tests.metadata.core;

fn add_bias(input: Sample, bias: Bias,
            first: i32, second: i32, third: i32,
            fourth: i32, fifth: i32) -> Sample {
    return {
        value = input.value + (bias as i32) + fifth,
    };
}

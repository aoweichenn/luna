module tests.switch_duplicate_case;

fn main() -> i32 {
    let value: u8 = 0;
    switch (value) {
        case -1 {
            return 1;
        }

        case 255 {
            return 2;
        }

        default {
            return 0;
        }
    }
}

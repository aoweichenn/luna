module test.memory_ir;

fn main() -> i32 {
    var values: [2]i32 = {};
    values[1] = 42;
    let base: *i32 = &values[0];
    base[0] = values[1];
    let text: *const u8 = "A";
    if (base != null && text[0] == 65) {
        return base[0];
    }
    return 0;
}

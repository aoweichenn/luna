module tests.aggregate_ir;

struct Pair {
    byte: u8;
    value: i32;
}

fn main() -> i32 {
    var pair: Pair = {};
    pair.value = 42;
    let pointer: *Pair = &pair;
    return pointer->value;
}

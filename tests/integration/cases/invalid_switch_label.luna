module tests.invalid_switch_label;

fn main() -> i32 {
    let value: i32 = 0;
    switch (value) {
        case value {
            return 1;
        }

        default {
            return 0;
        }
    }
}

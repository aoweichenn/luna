module tests.readonly_aggregate_field;

struct Item {
    value: i32;
}

fn main() -> i32 {
    var item: Item = {};
    let pointer: *const Item = (&item) as *const Item;
    pointer->value = 1;
    return 0;
}

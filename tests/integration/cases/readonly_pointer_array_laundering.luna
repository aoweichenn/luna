module test.readonly_pointer_array_laundering;

fn main() -> i32 {
    var value: i32 = 1;
    var entries: [1]*const i32 = {};
    entries[0] = (&value) as *const i32;
    let source: *[1]*const i32 = &entries;
    let invalid: *[1]*i32 = source as *[1]*i32;
    return 0;
}

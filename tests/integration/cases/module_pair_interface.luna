export module tests.module_pair;

export enum Bias: i32 {
    answer = 20,
}

export struct Input {
    value: i32;
    bias: Bias;
}

export extern fn c_i32_identity(value: i32) -> i32;

fn combine(input: *const Input, delta: i32) -> i32;

export fn main() -> i32;

module tests.union_initializer_multiple_fields;

union Number {
    integer: i64;
    real: f64;
}

fn main() -> i32 {
    var number: Number = {
        integer = 42,
        real = 42.0,
    };
    return 0;
}

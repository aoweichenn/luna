module tests.switch_label_overflow;

fn main() -> i32 {
    let value: i8 = 0;
    switch (value) {
        case 128 {
            return 1;
        }

        default {
            return 0;
        }
    }
}

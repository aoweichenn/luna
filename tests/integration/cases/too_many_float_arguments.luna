module tests.too_many_float_arguments;

fn too_many(
    a: f64,
    b: f64,
    c: f64,
    d: f64,
    e: f64,
    f: f64,
    g: f64,
    h: f64,
    i: f64
) -> f64 {
    return a + b + c + d + e + f + g + h + i;
}

fn main() -> i32 {
    return 0;
}

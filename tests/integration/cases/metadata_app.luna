module tests.metadata.app;

import tests.metadata.core;
import tests.metadata.math;

fn main() -> i32 {
    let input: Sample = { value = 40, };
    let result: Sample = calculate(input);
    return result.value;
}

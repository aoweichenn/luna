module tests.fail_map_nontrivial;

import luna.std.map::{map};

class Resource {
    pub init();
    pub deinit();
}

impl Resource {
    init() {
    }

    deinit() {
    }
}

fn main() -> i32 {
    let values: map<i32, Resource> = map<i32, Resource>();
    return values.empty() ? 0 : 1;
}

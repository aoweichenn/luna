module tests.signed_arithmetic;

fn main() -> i32 {
    let quotient: i32 = -17 / 5;
    let remainder: i32 = -17 % 5;
    let shifted: i32 = -16 >> 2;
    return (0 - quotient) * 10 + (0 - remainder) + (0 - shifted);
}

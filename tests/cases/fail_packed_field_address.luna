module tests.fail_packed_field_address;

@packed struct WireHeader {
    kind: u8;
    length: u32;
}

fn main() -> i32 {
    var backing: [8]u8 = {};
    let p: *WireHeader = (&backing) as *WireHeader;
    let length_pointer: *u32 = &p->length;
    return length_pointer == null ? 1 : 0;
}

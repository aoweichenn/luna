module tests.nested_call;

fn identity(value: i32) -> i32 {
    return value;
}

fn add(left: i32, right: i32) -> i32 {
    return left + right;
}

fn main() -> i32 {
    return add(identity(20), 22);
}

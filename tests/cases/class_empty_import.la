module tests.class_empty_import;

import tests.modules.empty_class as imported;

fn main() -> i32 {
    let marker: *imported::Marker = null;
    if (sizeof(imported::Marker) != 1 || alignof(imported::Marker) != 1) {
        return 1;
    }
    return marker == null ? 42 : 2;
}

module tests.fail_list_nontrivial;

import luna.std.list::{list};

class Resource {
    pub init();
    pub deinit();
}

impl Resource {
    init() {
    }

    deinit() {
    }
}

fn main() -> i32 {
    let values: list<Resource> = list<Resource>();
    return values.empty() ? 0 : 1;
}

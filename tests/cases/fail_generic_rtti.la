module tests.fail_generic_rtti;

@rtti
class Box<Value> {
    pub init(value: Value);
    pub virtual fn get() const -> Value;
}

fn main() -> i32 {
    let value: *Box<i32> = null;
    return value == null ? 0 : 1;
}

module tests.fail_const_fn_body;

const fn loop_total(n: i32) -> i32 {
    var total: i32 = 0;
    while (n > 0) {
        total = total + n;
        n = n - 1;
    }
    return total;
}

fn main() -> i32 {
    return 0;
}

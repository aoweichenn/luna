module tests.fail_class_rtti_query;

class Value {
    pub init();
    pub virtual fn read() const -> i32;
}

impl Value {
    init() {
    }

    fn read() const -> i32 {
        return 0;
    }
}

fn main() -> i32 {
    let value: Value = Value();
    return @type_is(&value, Value) ? 1 : 0;
}

module tests.fail_nontrivial_union;

class Guard {
    pub init();
    pub deinit();
}

impl Guard {
    init() {}
    deinit() {}
}

union Storage {
    guard: Guard;
    raw: u64;
}

fn main() -> i32 {
    let storage: Storage = {guard = Guard()};
    return 0;
}

module tests.bool_call;

fn select(condition: bool, left: i32, right: i32) -> i32 {
    if (condition) {
        return left;
    }
    return right;
}

fn main() -> i32 {
    return select(true, 42, 1);
}

module tests.fail_export_name_duplicate;

@export_name("c_dup") fn first(x: i32) -> i32 {
    return x + 1;
}

@export_name("c_dup") fn second(x: i32) -> i32 {
    return x + 2;
}

fn main() -> i32 {
    return first(1) + second(2);
}

export module tests.fail_asm_fn_interface;

// asm fn is an implementation-unit feature; declaring one in an interface
// unit is invalid_asm_function (the diagnostic leads even though the unit
// also lacks an entry point for --executable).
export asm fn naked() -> i32 {
    "movl $42, %eax\n"
    "ret\n"
}

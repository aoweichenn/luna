module tests.fail_vector_nontrivial;

import luna.std.vector::{vector};

class Resource {
    pub init();
    pub deinit();
}

impl Resource {
    init() {
    }

    deinit() {
    }
}

fn main() -> i32 {
    let values: vector<Resource> = vector<Resource>();
    return values.empty() ? 0 : 1;
}

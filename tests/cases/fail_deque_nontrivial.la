module tests.fail_deque_nontrivial;

import luna.std.deque::{deque};

class Resource {
    pub init();
    pub deinit();
}

impl Resource {
    init() {
    }

    deinit() {
    }
}

fn main() -> i32 {
    let values: deque<Resource> = deque<Resource>();
    return values.empty() ? 0 : 1;
}

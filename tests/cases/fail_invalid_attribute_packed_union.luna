module tests.fail_invalid_attribute_packed_union;

@packed union Bits {
    bytes: [4]u8;
    word: u32;
}

fn main() -> i32 {
    var bits: Bits = {};
    bits.word = 42;
    return bits.bytes[0] as i32;
}

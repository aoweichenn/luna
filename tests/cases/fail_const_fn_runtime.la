module tests.fail_const_fn_runtime;

const fn align_up(value: usize, alignment: usize) -> usize {
    return (value + alignment - 1) & ~(alignment - 1);
}

fn main() -> i32 {
    var aligned: usize = align_up(1, 64);
    return aligned as i32;
}

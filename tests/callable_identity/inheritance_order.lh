export module ci.inheritance_order;

export class Base {
    pub virtual fn value(input: i32) const -> i32;
}

export class Derived : Base {
    pub override final fn value(input: i32) const -> i32;
}

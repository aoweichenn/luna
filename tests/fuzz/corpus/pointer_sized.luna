module fuzz.pointer_sized;

fn calculate(offset: isize, size: usize) -> usize {
    let adjusted: isize = (offset * 3) >> 65;
    return (adjusted as usize) + size;
}

fn main() -> i32 {
    if (calculate(-8, 20) == 8) {
        return 42;
    }
    return 1;
}

export module tests.modules.class_api;

export class Gauge {
    priv stored: i32;

    pub init(value: i32 = 40);
    pub fn value() const -> i32;
    pub fn bump(amount: i32 = 2);
    pub static fn answer() -> i32;
}

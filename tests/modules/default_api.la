module tests.modules.default_api;

fn combine(input: i32, added: i32) -> i32 {
    return input + added;
}

fn pointer_value(pointer: *const i32) -> i32 {
    return pointer == null ? 40 : *pointer;
}

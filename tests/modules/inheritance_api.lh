export module tests.modules.inheritance_api;

export class Base {
    prot stored: i32;

    pub init(value: i32);
    pub fn choose(value: i32) const -> i32;
    pub virtual fn dynamic_value() const -> i32;
}

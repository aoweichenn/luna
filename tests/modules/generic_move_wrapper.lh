export module tests.modules.generic_move_wrapper;

export class wrapper<Value> {
    priv stored: Value;

    pub init(value: Value&&);
    pub deinit();
}

impl wrapper {
    init(value: Value&&): stored = value as Value&& {
    }

    deinit() {
    }
}
